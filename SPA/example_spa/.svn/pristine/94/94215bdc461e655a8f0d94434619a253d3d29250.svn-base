define([], function () {
    'use strict';

    var link = function(url) {
        var css = document.createElement('link');
        css.rel = 'stylesheet';
        css.href = url;

        document.head.appendChild(css);
    };

    return {
        link: link
    };
    
});
